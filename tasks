Организация работы:
	--> Любой новый функционал имеет свою ветку разработки. например, server.py разрабатывается только в ветке server
	--> Ветки функционала отходят от главной ветки dev
	--> файлы cryptoex и <name> (e.g. moex) для работы с биржами должны содержать только файлы, обрабатывающие ответы бирж и возвращающие результат
	--> возможно, функции для работы с биржами лучше организовать в класс ( чтобы избежать возможного пересечения имён )
	--> схема обновления репозитория прописана в алгоритме от 22.11 (часть для collaborators)
